import { Text } from "@/components/Text";
import { View } from "react-native";

const SearchScreen = () => {
  return (
    <View className="flex-1 bg-yellow-500 items-center justify-center">
      <Text variant="body18Semibold">Search</Text>
    </View>
  );
};

export default SearchScreen;

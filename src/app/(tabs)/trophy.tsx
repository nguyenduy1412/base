import { Text } from "@/components/Text";
import { View } from "react-native";

const TrophyScreen = () => {
  return (
    <View className="flex-1 bg-pink-500 items-center justify-center">
      <Text variant="body18Semibold">Trophy</Text>
    </View>
  );
};

export default TrophyScreen;

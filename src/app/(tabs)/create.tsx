import { Text } from "@/components/Text";
import { View } from "react-native";

const CreateScreen = () => {
  return (
    <View className="flex-1 bg-black items-center justify-center">
      <Text variant="body18Semibold">Create</Text>
    </View>
  );
};

export default CreateScreen;
